export interface TaskStructure {
  description: string;
  status: boolean;
  id: string;
}

export interface AddTask {
  description: string;
}

export interface uiStructure {
  loading: boolean;
}
