import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#F06449",
      dark: "#EDE6E3",
      light: "#DADAD9",
      contrastText: "#36382e",
    },
  },
});

export default theme;
